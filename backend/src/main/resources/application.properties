spring.application.name=smart-campus-operations-hub

# MongoDB Connection (Atlas Cloud)
spring.data.mongodb.uri=mongodb+srv://manjikavi8_db_user:Kavishan1234@cluster0.vxsfzun.mongodb.net/smartcampus_db?authSource=admin&appName=Cluster0
# Run the backend server on port 8000
server.port=8000

# File upload settings
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=30MB
image.upload.dir=./uploads

# Spring Security - disable default login page / permit all for now
spring.security.user.password=disabled

# JWT Configuration (required by JwtService)
app.jwt.secret=smartcampus-secret-key-at-least-32-characters-long-for-hs256
app.jwt.expiration-ms=86400000
